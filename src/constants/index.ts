import areaList from "./area";
const ACCESS_TOKEN = 'access_token';
const REFRESH_TOKEN = 'refresh_token';
const USER = 'user';
const BASE_URI = "http://localhost:5000/api/v1"

export { ACCESS_TOKEN, REFRESH_TOKEN, USER, BASE_URI, areaList };
